import styled from 'styled-components'
import {theme} from '../../StyledMain'
import {Link} from 'react-router-dom'

export const StyledCategory = styled.h1`
    color:  ${theme.$pre};
    background: ${theme.$primary};
    margin:1rem ;
    border-radius: 1rem;
    color: ${theme.$pre};
    font-size: 1.5rem;
    padding:.6rem;
`